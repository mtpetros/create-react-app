const path = require('path')

// Define your own path aliases here. E.g.:
// { 'Components': path.resolve(__dirname, 'src/components') }

const aliases = {
  'Src': path.resolve(__dirname, 'src')
}

module.exports.aliases = aliases
